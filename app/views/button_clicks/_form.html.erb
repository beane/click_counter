<%= form_for(@button_click) do |f| %>
  <% if @button_click.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@button_click.errors.count, "error") %> prohibited this button_click from being saved:</h2>

      <ul>
      <% @button_click.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
